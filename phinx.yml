paths:
  migrations: db/migrations
  seeds: db/seeds

environments:
  default_migration_table: phinxlog
  default_environment: development
  development:
    adapter: pgsql
    host: db
    name: api_db
    user: user
    pass: password
    port: 5432
    charset: utf8
